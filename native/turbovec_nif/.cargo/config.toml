[target.'cfg(target_arch = "x86_64")']
rustflags = ["-C", "target-cpu=x86-64-v2"]

# musl defaults to static CRT, which cannot produce a cdylib
[target.'cfg(target_env = "musl")']
rustflags = ["-C", "target-feature=-crt-static"]
